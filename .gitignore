# .gitignore - tells git whcih files to never push to github

# local enviorment var files
#if a .env files are ever created for local tetsing, 
#this stops it form being pushed to Github

.env
.env.local
.env.*

# dependencies 
#without node_modules/ repo can get huge and installs can break across enviorments
node_modules/

#Netlify local cache (created when 'netlify dev' is ran)
.netlify/

#Mac systems files - not apart of this project
.DS_Store

#Window system files
Thumbs.db